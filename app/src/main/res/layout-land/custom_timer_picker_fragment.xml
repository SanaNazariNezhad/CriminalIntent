<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".controller.fragment.TimePickerFragment">

    <ImageView
        android:id="@+id/imgView_clock"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        app:srcCompat="@drawable/ic_clock" />

    <TextView
        android:id="@+id/txtView_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:gravity="center"
        android:textStyle="bold"
        android:textColor="@color/black"
        android:textSize="18sp"
        android:layout_toEndOf="@id/imgView_clock"
        android:layout_alignBottom="@id/imgView_clock"
        android:text="@string/time_picker_title" />

    <TimePicker
        android:id="@+id/time_picker_crime"
        android:layout_width="250dp"
        android:layout_height="150dp"
        android:layout_below="@id/imgView_clock"
        android:scaleX="0.7"
        android:scaleY="0.7"
        android:timePickerMode="spinner">

    </TimePicker>

    <Button
        android:id="@+id/btn_cancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/time_picker_crime"
        android:background="@color/white"
        android:layout_alignParentLeft="true"
        android:backgroundTint="@color/white"
        android:textColor="@color/background"
        android:text="@android:string/cancel" />

    <Button
        android:id="@+id/btn_ok"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/btn_cancel"
        android:layout_below="@id/time_picker_crime"
        android:background="@color/white"
        android:textColor="@color/background"
        android:text="@android:string/ok" />

</RelativeLayout>